{
  "name": "my-app",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "test": "bun test src/tests/*",
    "client": "bun run src/client.mts",
    "build": "bun build src/server.ts --outdir dist"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.0",
    "@hono/zod-openapi": "^0.18.4",
    "@hono/zod-validator": "^0.4.2",
    "hono": "^4.6.20",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}